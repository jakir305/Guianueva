import 'package:flutter/material.dart';

class ListaDeCarreras extends StatelessWidget {
  const ListaDeCarreras({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
